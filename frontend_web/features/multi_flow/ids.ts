// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// Button IDs (for click event logging)
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
export const InputScreenButtonIds = {
  next: "multi-flow-input-next-button",
} as const;

export const ConfirmScreenButtonIds = {
  back: "multi-flow-confirm-back-button",
  submit: "multi-flow-confirm-submit-button",
} as const;

export const CompleteScreenButtonIds = {
  backToInput: "multi-flow-complete-back-to-input-button",
} as const;

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// Test IDs (for non-button elements)
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
export const MultiFlowTestIds = {} as const;
