# Node.jsとpnpmのバージョンを厳格にチェック
# package.jsonのenginesフィールドで指定されたバージョンと異なる場合はエラーにする
engine-strict=true

# 依存関係の整合性を厳格にチェック
# package.jsonに記載されていない依存関係の使用を防ぐ
strict-peer-dependencies=true

# lockfileのみからインストール（package.jsonとの不整合を防ぐ）
frozen-lockfile=false

# 自動インストールを無効化（明示的なpnpm installを強制）
auto-install-peers=false

# shamefully-hoist を無効化（依存関係の巻き上げを防ぎ、厳格な依存解決）
shamefully-hoist=false

# 新規リリースから2週間経過したパッケージのみインストール可能（サプライチェーン攻撃対策）
minimum-release-age=2w
